// Code your solutions in this file
function writeCards(names, type) {
    for (let i = 0; i < names.length; i++) {
        console.log(`Thank you, ${names[i]} for the wonderful ${type[i]} gift!`);
    }
    return names;
}

